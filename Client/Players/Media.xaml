<UserControl x:Class="Client.Media"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="471" d:DesignWidth="503">
    <Grid x:Name="MediaGrid"
           VerticalAlignment="Stretch" 
      HorizontalAlignment="Stretch"
      >
            <Canvas x:Name="MediaCanvas"  Height="Auto" Width="Auto" Opacity="100" HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
            <!--<Canvas.Effect>
                <DropShadowEffect Color="Blue" Opacity="1" BlurRadius="5" ShadowDepth="0"/>
            </Canvas.Effect>-->
        </Canvas>

     

    </Grid>
</UserControl>
